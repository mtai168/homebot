# HomeBox Bot — 管理功能擴充 SPEC v1.0.0（FROZEN）

> 狀態：✅ 已凍結（2026-07-15，經 delegate 獨立審查 + 主代理過濾）
> 架構前提：單機獨立部署，一戶一庫存，成員共用同一份 data.json（架構 A）
> 互動模式：純文字指令為主；Telegram `/指令` 註冊 ASCII slug 供 ≡ 選單；破壞性操作走 inline keyboard 二次確認
> 本期不含：匯出/匯入(原6)、部署 README(原8)、audit 日誌(原9) — 暫不做
> 空庫存：不做動態引導，改為預置預設資料（§6）

---

## 1. 指令總表（實作後目標態）

### 1.1 Telegram `/指令`（≡ 選單，必須 ASCII slug）
> ⚠️ Bot API `BotCommand` 只收 `[a-z0-9_]`。中文指令無法註冊，故 ≡ 選單用 ASCII，中文保留在文字解析層。

| 指令 | 說明 | 對應中文文字指令 |
|------|------|----------------|
| /buy | 增加庫存 | 買/買了/補充/加入/新增 物品 數量 |
| /use | 減少庫存 | 用/用了/使用/消耗/拿走/取出 物品 數量 |
| /view | 列出物品 | 查看 [分類/位置] |
| /alert | 低庫存清單 | 告警 |
| /log | 買/用歷史 | 紀錄 [用/買] [天數]（異體字 記錄/纪录） |
| /help | 完整指令說明 | ? / ？ / help / 說明 |

### 1.2 管理指令（文字，不入 ≡ 選單，僅列於 /help）
| 指令格式 | 別名 | 作用 |
|---------|------|------|
| `新增分類 名稱` | `加分類` | 新增一個分類 |
| `重命名分類 舊 => 新` | `改分類名` | 分類改名 + cascade 引用 |
| `重命名位置 舊 => 新` | `改位置名` | 位置改名 + cascade 引用 |
| `重命名物品 舊 => 新` | `改物品名` | 物品改名 |
| `刪除物品 名稱` | `移除物品` | 刪物品（0 也確認；>0 確認） |
| `刪除分類 名稱` | `移除分類` | 嚴格閘門：無物品引用才可刪 |
| `刪除位置 名稱` | `移除位置` | 嚴格閘門：無物品有庫存引用才可刪 |

### 1.3 查詢/說明
`?` / `？` / `help` / `說明` / `/help` / `/?` → 完整分組指令說明頁

---

## 2. 各功能規格

### 2.1 新增分類 `新增分類 名稱`
- 參數：取 `新增分類 ` 後全部為名稱（可含空格）
- 校驗：已存在 → `❌ 分類「X」已存在`
- 動作：`data["categories"].append(名稱)`；`save_data`
- 回覆：`✅ 已新增分類「X」（目前共 N 個）`
- CATEGORY_EMOJI 無該分類 → 顯示用預設 📦，不報錯

### 2.2 重命名（分類/位置/物品）— `舊 => 新` 分隔
格式：`重命名{物件} 舊名 => 新名`
- 解析：`text.split("=>", 1)` → 舊=前半去空白、新=後半去空白（舊/新名可含空格，不含 `=>` 即可）
- 舊名**精確比對**（管理操作不模糊）
- 找不到舊名 → `❌ 找不到{物件}「X」`
- 新名與同類型現有名稱重複 → `❌ 「新名」已存在，無法重命名`
- cascade：
  - 分類：所有 `item["category"]==舊` → 改 `新`
  - 位置：所有 `item["locations"][].name==舊` → 改 `新`；`locations_index` 舊→新
  - 物品：`item["name"]==舊` → 改 `新`
- 回覆：`✅ 已將 {物件}「舊」改名為「新」（影響 N 筆）`
- **歷史不改寫**：transactions.jsonl 保留原名（審計用途），不回寫

### 2.3 刪除物品 `刪除物品 名稱`
- 物件精確比對（全名，可含空格）
- 找不到 → `❌ 找不到物品「X」`
- 無論數量 0 或 >0：**皆走 inline keyboard 二次確認**（是/否），防手滑
  - 確認訊息含目前數量：`「X」目前有 N 個，確定刪除？`
- 確認後：`data["items"].remove(item)`；`save_data`；`✅ 已刪除「X」`
- 否：編輯原訊息 `❌ 已取消`

### 2.4 刪除分類 `刪除分類 名稱` — 嚴格閘門
- 不在 `categories` → `❌ 找不到分類「X」`
- 引用檢查：`refs = [i for i in items if i["category"]==名稱]`
- `refs` 非空 → 拒絕：`⚠️ 仍有 N 個物品屬於「X」，請先改其分類或刪除它們`
- 空 → inline 確認 → 從 `categories` 移除 → `✅ 已移除分類「X」`

### 2.5 刪除位置 `刪除位置 名稱` — 嚴格閘門
- 不在 `locations_index` → `❌ 找不到位置「X」`
- 引用檢查（只算有庫存）：`refs = [i for i in items for l in i["locations"] if l["name"]==名稱 and l["quantity"]>0]`
- `refs` 非空 → 拒絕：`⚠️ 位置「X」仍有 N 個物品有庫存，請先歸零或移走`
- 空 → inline 確認 → 從每個物品的 `locations[]` 拔除同名條目（含 qty==0）、從 `locations_index` 移除 → 重算各物品 `total` → `✅ 已移除位置「X」`
- 拔除後物品 `locations[]` 可能變空 → 保留空陣列，total 重算為 0（不刪物品本身）

### 2.6 `/help` 說明頁（動態產生）
- 由程式內 `COMMANDS` 清單產生（說明頁與 §1.1 註冊共用同一定義，防漂移）
- 分組：①常用操作（buy/use/view/alert/log 中文格式+範例）②管理（新增分類/重命名×3/刪除物品/刪除分類/刪除位置）③說明
- 管理指令標註「僅文字指令，不在 ≡ 選單」
- 總長 > 4096 字 → 沿用現有分段邏輯

### 2.7 Telegram `/指令` 註冊（ASCII slug）
- 啟動時經 `Application.post_init` async hook 呼叫 `application.bot.set_my_commands([...])`（PTB 22.6 支援；不可在同步 main 裸呼叫需 await）
- 清單：/buy /use /view /alert /log /help
- 失敗只 log，不阻擋啟動

---

## 3. 預置預設資料（替代空庫存引導）
- 預置檔：`/root/homebox/data.seed.json`（結構同 data.json）
- 部署腳本**僅在 `data.json` 不存在時** `cp data.seed.json data.json`，絕不覆蓋既有
- 同時把 `load_data()` 的 default 也改為含預置（檔案不存在時生成，不影響現有線上檔）：
  ```python
  default = {"items":[{"id":1,"name":"空氣","category":"日常用品","alert_threshold":0,
            "locations":[{"name":"儲藏室","quantity":1}],"total":1,
            "created_at":now,"updated_at":now}],
            "categories":["日常用品"],"locations_index":["儲藏室"],"next_id":2}
  ```
- 預置僅為避免空庫存報錯，使用者可自由重命名/刪除

---

## 4. 設計決策（已拍板）
- D1 互動：≡ 選單只放 6 個 ASCII slug；管理指令不入 ≡，僅 /help + 文字
- D2 破壞性確認：統一 inline keyboard 二次確認（是/否）
- D3 刪除物品即使數量 0 也確認
- D4 重命名精確比對；分隔符 `=>`（非最後空格切分）
- D5 分類/位置刪除嚴格閘門：必須無（有庫存）引用才可刪，不 auto-move
- D6 `total` 為衍生欄位，以 `locations[]` 為準，所有寫入路徑重算
- D7 重命名不改寫 transactions.jsonl（保留歷史原名）
- D8 管理指令**僅在非對話流程狀態可用**（使用者未在買/用流程中）；對話中輸入管理指令不被處理（與現有行為一致，本期不做全域 escape）

---

## 5. 實作分批
- **批次 1（低風險）**：§2.1 新增分類 + §2.6 /help 說明頁 + §2.7 /指令註冊 + §3 預置 seed + `load_data` default 改預置 + `COMMANDS` 常數
- **批次 2（破壞性，謹慎）**：§2.2 重命名×3 + §2.3 刪除物品 + §2.4/2.5 刪除分類/位置（含 inline 確認 + cascade + total 重算）

---

## 6. 已知限制（本期不處理）
- 無匯出/匯入、無部署 README、無 audit 日誌
- 無 legacy corruption 自動修復（依賴預置 + 不變量）
- 對話流程中不支援管理指令（D8）
